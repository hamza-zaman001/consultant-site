// script.js

// Function to handle the fade-in effect
window.addEventListener('load', () => {
    document.querySelectorAll('.intro, .solutions').forEach(element => {
        element.style.opacity = '1';
    });
});
